# ── Node / Next.js (app/) ────────────────────────────────────────────
app/node_modules/
app/.next/
app/out/
app/.vercel/
app/*.tsbuildinfo
app/next-env.d.ts
app/.env.local
app/.env*.local

# ── Python / Training (training/) ───────────────────────────────────
training/venv/
training/__pycache__/
training/**/__pycache__/
training/*.pyc
training/checkpoints/
training/data/audio/
training/data/features/
training/data/manifest.json
training/data/test_split.json
training/benchmark_results.json
*.egg-info/

# ── LaTeX (report/) ──────────────────────────────────────────────────
report/*.aux
report/*.bbl
report/*.blg
report/*.log
report/*.out
report/*.synctex.gz
report/*.fdb_latexmk
report/*.fls

# ── OS / Editor ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ── Secrets — never commit ───────────────────────────────────────────
*.env
.env
.env.local
**/secrets.json
**/*token*.txt
app/.env.production
hf_space/__pycache__/