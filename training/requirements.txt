# SpeakPay local fine-tuning — pinned dependency versions
# Tested combo that avoids the transformers/peft/Whisper incompatibilities
# hit during Colab attempts (input_ids mismatch, proj_out shape mismatch).
#
# Do NOT pip install --upgrade individual packages — version drift here
# is exactly what caused repeated failures in Colab.

transformers==4.35.2
peft==0.6.2
accelerate==0.24.1
datasets>=2.19.0,<3.0.0
evaluate>=0.4.0
jiwer>=3.0.3
librosa>=0.10.0
soundfile>=0.12.0
huggingface_hub>=0.20.0,<1.0
numpy<2.0.0
